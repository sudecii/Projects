# 🔐 CAPTCHA Recognition (Kaggle SF Competition)

**Цель проекта.**  
Построить модель компьютерного зрения, которая распознаёт символы на CAPTCHA-изображениях (один символ на картинку) и формирует корректный `submission.csv` для соревнования **SF CAPTCHA Recognition** на Kaggle.

Модель — сверточная нейронная сеть (CNN) с аугментацией данных. Итоговый результат на Kaggle: **0.85070 Public Score**.
Уменьшил количество эпох в обучении, поэтому качество наверное упадет. Но это демонстративный ноутбук, результат есть на Каггл.
---

## 📌 Возможности

- Полный ML-пайплайн в одном Jupyter-ноутбуке:
  - загрузка и исследование данных;
  - нормализация изображений;
  - train/validation split;
  - построение CNN-модели;
  - аугментация изображений;
  - визуализация метрик;
  - генерация submission-файла.
- Работает как локально, так и на Kaggle.
- Финальная модель стабильно обобщает и показывает высокое качество.

---

## 🧠 Данные

Соревнование предоставляет:

| Файл | Описание |
|------|----------|
| `images.npy` | тренировочные изображения `48×48×3` |
| `labels.npy` | метки классов (0–25) |
| `images_unlabeled.npy` | необозначенные изображения |
| `images_sub.npy` | тестовый набор (50 000 изображений) |

---

## 🏗 Архитектура модели

- **Вход:** `48×48×3`
- **Аугментация:**
  - `RandomRotation(0.05)`
  - `RandomTranslation(0.1, 0.1)`
  - `RandomZoom(0.1)`
- **CNN-блоки:**
  - `Conv2D(32, 3, relu)`
  - `Conv2D(64, 3, relu)`
  - `MaxPool2D`
  - `Conv2D(128, 3, relu)`
  - `Conv2D(128, 3, relu)`
  - `MaxPool2D`
- **Head:**
  - `Flatten`
  - `Dense(256, relu)`
  - `Dropout(0.15)`
  - `Dense(26, softmax)`

---

## 📈 Результаты обучения

- **Train Accuracy:** ~0.75  
- **Validation Accuracy:** ~0.84  
- **Train Loss:** уверенно убывает  
- **Validation Loss:** стабильный спад, без признаков переобучения  

---

## 📊 Результаты на тестовом наборе (Kaggle)

После генерации `submission.csv` и загрузки:

> ✔ **Public Score: 0.85070**

Это означает, что приблизительно **85% символов** предсказаны верно.


## 🚀 Установка и запуск локально

```bash
git clone https://github.com/<your_username>/captcha-cnn-kaggle.git
cd captcha-cnn-kaggle

python -m venv venv
source venv/bin/activate    # Windows: venv\Scripts\activate

pip install -r requirements.txt